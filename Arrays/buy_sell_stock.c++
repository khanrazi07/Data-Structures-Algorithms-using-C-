#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main(){

    vector <int> vect = {7,1,5,3,6,4};
    // here we have to make sure that our profit is max 
    int n = vect.size();
    int minElement = *min_element(vec.begin(), vec.end());
    int sellday ;
    // for(int i = 0 ; i < vect.size() ; i++){
    //     if(vect[i] == )
    // }
    cout<<minElement<<endl;
}