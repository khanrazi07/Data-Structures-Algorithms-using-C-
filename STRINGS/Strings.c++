#include<iostream>
#include<string>
using namespace std;

int main(){
    string str = "Hi Razi Khan"; // now this stringg is assinged in the array with in last ('\0) this special character
    cout<<str<<endl;
    string str1 = "lets do some code";
    // now we apply some operations on the strings , lets do the concatinate the strings
    string str2 = str +" " + str1;
    cout<<str2<<endl;
    
    
}